{
  "name": "@ladc/mysql2-adapter",
  "version": "0.22.9",
  "description": "Connect to MySQL and MariaDB with the LADC API.",
  "main": "ladc-mysql2-adapter.min.js",
  "types": "ladc-mysql2-adapter.d.ts",
  "dependencies": {
    "mysql2": "^2.1.0"
  },
  "peerDependencies": {
    "ladc": "0.22.7"
  },
  "devDependencies": {
    "ladc": "^0.22.7",
    "npm-run-all": "4",
    "rimraf": "^3.0.1",
    "rollup": "^1.31.0",
    "terser": "^4.6.3",
    "tslint": "^6.0.0",
    "typescript": "^3.7.5"
  },
  "scripts": {
    "build": "run-s clear tsc make-bundle",
    "tsc": "tsc",
    "tsc:watch": "tsc --watch",
    "make-bundle": "node build/make-bundle",
    "clear": "rimraf build/compiled/*",
    "lint": "tslint -p tsconfig.json -t verbose",
    "prepublishOnly": "npm run lint && npm run build"
  },
  "engines": {
    "node": ">=8"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/paleo/ladc.git"
  },
  "homepage": "https://github.com/paleo/ladc/tree/master/mysql2-adapter",
  "author": {
    "name": "Paleo"
  },
  "license": "CC0-1.0",
  "keywords": [
    "mysql",
    "mariadb",
    "connection",
    "ladc"
  ]
}
