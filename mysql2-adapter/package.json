{
  "name": "@ladc/mysql2-adapter",
  "version": "0.22.11",
  "description": "Connect to MySQL and MariaDB with the LADC API.",
  "main": "ladc-mysql2-adapter.min.js",
  "types": "ladc-mysql2-adapter.d.ts",
  "dependencies": {
    "mysql2": "^2.1.0"
  },
  "peerDependencies": {
    "ladc": "0.22.7"
  },
  "devDependencies": {
    "@types/node": "^12.12.44",
    "ladc": "^0.22.7",
    "npm-run-all": "4",
    "rimraf": "^3.0.2",
    "rollup": "^2.15.0",
    "terser": "^4.7.0",
    "tslint": "^6.1.2",
    "typescript": "^3.9.5"
  },
  "scripts": {
    "build": "run-s clear tsc make-bundle",
    "tsc": "tsc",
    "tsc:watch": "tsc --watch",
    "make-bundle": "node build/make-bundle",
    "clear": "rimraf build/compiled/*",
    "lint": "tslint -p tsconfig.json -t verbose",
    "prepublishOnly": "npm run lint && npm run build"
  },
  "engines": {
    "node": ">=8"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/paroi-tech/ladc.git"
  },
  "homepage": "https://github.com/paroi-tech/ladc/tree/master/mysql2-adapter",
  "author": {
    "name": "Paleo"
  },
  "license": "CC0-1.0",
  "keywords": [
    "mysql",
    "mariadb",
    "connection",
    "ladc"
  ]
}